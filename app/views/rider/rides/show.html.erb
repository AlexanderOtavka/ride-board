<%= render "rides/error_explanation", problem: @problem, errors: @errors %>

<p>
  <strong>Start location:</strong>
  <%= @ride.start_location.name %>
</p>

<p>
  <strong>Start datetime:</strong>
  <%= @ride.start_datetime %>
</p>

<p>
  <strong>End location:</strong>
  <%= @ride.end_location.name %>
</p>

<p>
  <strong>End datetime:</strong>
  <%= @ride.end_datetime %>
</p>

<p>
  <strong>Driver:</strong>
  <%= @ride.driver ? @ride.driver.email : "None" %>
</p>

<p>
  <strong>Price:</strong>
  <%= @ride.price %>
</p>

<p>
  <strong>Seats remaining:</strong>
  <%= seats_remaining @ride %>
</p>

<% unless @ride.driver == current_user %>
  <% if @ride.has_passenger? current_user %>
    <%= link_to 'Leave', rider_join_ride_path(@ride), method: :delete %> |
  <% else %>
    <%= link_to 'Join', rider_join_ride_path(@ride), method: :post %> |
  <% end %>
<% end %>

<%= link_to 'Edit', edit_rider_ride_path(@ride) %> |
<%= link_to 'Back', rider_rides_path %>
