<!DOCTYPE html>
<html lang="en" prefix="og: http://ogp.me/ns#">
  <head>
    <meta charset="utf-8">
    <title>RideBoard.app</title>

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta property="og:site_name" content="RideBoard.app" />
    <meta property="fb:app_id" content="2479083255670794" />

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_include_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <%= yield(:head) %>

    <%# This is below the yield so pages can override it %>
    <meta property="og:image" content="<%= image_url 'splash.jpg' %>" />
  </head>

  <body>
    <% flash.each do |message_type, message| %>
      <%= javascript_tag "M.toast({html: '#{j(h(message))}'}, 4000)" %>
    <% end %>

    <%= content_for?(:body) ? yield(:body) : yield %>
  </body>
</html>
