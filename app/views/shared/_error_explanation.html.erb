<% if !errors.nil? && errors.any? %>
  <div id="error_explanation">
    <h2><%= problem %> (<%= pluralize(errors.count, "error") %>):</h2>

    <ul>
      <% errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
